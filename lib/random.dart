import 'package:flutter/material.dart';

import 'package:app1/display.dart';

class Random extends StatelessWidget {
  const Random({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Display());
  }
}
