import 'package:flutter/material.dart';

class CreateFruitsScreen extends StatelessWidget {
  const CreateFruitsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
